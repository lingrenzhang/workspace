
	* General (login, info update, etc)
	* 
		* landing page url: can we change it to 5udache.com? (high)
		* forget password  (high) 
		* email notification (high)

	* Transient ride
	* 
		* available seats not initialized when postTransientRide (high)
		* landing page for successfully added rides, then return to the default landing page (high)
		* Use hover to provide comment that help user know details (high)
		* shouldn't let user select times earlier than current time (high)
		* user should be able to cancel the cancel action (maybe fat finger) (high, combine with 10, 12, 18)
		* taxi, probably don't specify providing cars and money, just the number of seats left, change the money to distance  (high)
		* day click should pop up selection panel when clicking on the text field (high)
		* when delete the day string, then click on the calendar logo, bug happens! (high)
		* when searching with no result, should pop up some notification + notification of searching (high)

	* Mobile 
		* Name of the app... need to be consistent with the web version (5udache?) (high)
		* search for ride?? no input boxes (time logic) (high) 
		* the menu button sometimes don't work, dont respond when click (for example, don't respond when login on the landing page) (high)



